<metro:Window x:Class="Elysium.Test.MainWindow"
               xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
               xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
               xmlns:metro="http://schemas.codeplex.com/elysium"
               xmlns:params="http://schemas.codeplex.com/elysium/params"
               params:Manager.Theme="Dark"
               params:Manager.AccentBrush="{x:Static metro:AccentBrushes.Blue}"
               params:Manager.ContrastBrush="White"
              Background="{DynamicResource BackgroundBrush}" Width="500" Height="400" Title="TinyClient">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <StackPanel Orientation="Horizontal">
        </StackPanel>
    </Grid>
    <metro:Window.TitleBar>
        <Grid>
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <Button Grid.Column="1" Content="Аудио" Style="{DynamicResource {x:Static metro:Window.DefaultCaptionButtonStyleKey}}"/>
                <Button Grid.Column="1" Content="Друзья" Style="{DynamicResource {x:Static metro:Window.DefaultCaptionButtonStyleKey}}"/>
                <Button Grid.Column="1" Content="Новости" Style="{DynamicResource {x:Static metro:Window.DefaultCaptionButtonStyleKey}}"/>
            </StackPanel>
        </Grid>
    </metro:Window.TitleBar>
</metro:Window>
